# Solidity ABI Specification

The Ethereum Application Binary Interface defines how contract calls are encoded and how return values and events are decoded. Developer tools use ABIs to construct smart contract calls and interpret logs.
